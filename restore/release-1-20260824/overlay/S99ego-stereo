#!/bin/sh
# Boot: start Ego stereo web service. Never change the USB gadget.
# $1 is "start" from rcS. Detach with setsid so this survives sysinit.
[ "$1" = "start" ] || [ -z "$1" ] || exit 0
chmod 755 /userdata/camevision-stereo.sh 2>/dev/null
if [ ! -x /userdata/camevision-stereo.sh ]; then
	exit 0
fi
setsid /bin/sh -c 'sleep 8; exec /userdata/camevision-stereo.sh' \
	</dev/null >/userdata/ego-stereo-boot.log 2>&1 &
exit 0
